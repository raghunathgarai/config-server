spring:
  application:
    name: department-service
  datasource:
    url: jdbc:mysql://localhost:3306/microservices_11_2025
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081
